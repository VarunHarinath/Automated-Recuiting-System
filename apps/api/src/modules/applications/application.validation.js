import{ApplicationStatus}from'@prisma/client';import{z}from'zod';import{parseInput}from'../users/user.validation.js';const uuid=z.string().uuid();const nullableText=max=>z.string().trim().max(max).nullable().optional();
export const applicationCreateSchema=z.object({candidateId:uuid,jobId:uuid,assignedRecruiterId:uuid.nullable().optional(),source:nullableText(100),notes:z.string().trim().min(1).max(5000).optional()}).strict();
export const applicationUpdateSchema=z.object({assignedRecruiterId:uuid.nullable().optional(),source:nullableText(100)}).strict().refine(v=>Object.keys(v).length>0,{message:'At least one field must be provided.'});
export const applicationStatusSchema=z.object({status:z.nativeEnum(ApplicationStatus),reason:z.string().trim().max(2000).optional()}).strict();
export const applicationNoteSchema=z.object({content:z.string().trim().min(1).max(5000)}).strict();export const applicationIdSchema=z.object({id:uuid}).strict();
export const applicationListSchema=z.object({page:z.coerce.number().int().min(1).default(1),limit:z.coerce.number().int().min(1).max(100).default(20),status:z.nativeEnum(ApplicationStatus).optional(),jobId:uuid.optional(),candidateId:uuid.optional(),assignedRecruiterId:uuid.optional(),dateFrom:z.coerce.date().optional(),dateTo:z.coerce.date().optional(),search:z.string().trim().min(1).max(100).optional()}).strict().superRefine((v,c)=>{if(v.dateFrom&&v.dateTo&&v.dateTo<v.dateFrom)c.addIssue({code:'custom',path:['dateTo'],message:'dateTo cannot precede dateFrom.'});});export{parseInput};
